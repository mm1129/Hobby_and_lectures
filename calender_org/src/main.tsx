import React from 'react';
import ReactDOM from 'react-dom/client';
import MorningDashboard from './all_in_one_morning_calendar_ui_template_next_js_react';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <MorningDashboard />
    </React.StrictMode>
);